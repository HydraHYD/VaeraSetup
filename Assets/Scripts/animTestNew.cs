using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class animTestNew : MonoBehaviour
{

    // ALL VARIABLES
    int[] currentFrames; // current set of frame positions used
    int framePos; // current position in animation

    void Start() // DECLARE DEFAULT VARIABLE VALUES
    {

    }

    void Update()
    {
        // check if framePos is too large
        // if too large
        // set actions to false

        // check if there is a current action
        // if no action
        // check if entity is moving
        // check if entity is using action
        // if using action
        // reset framePos

        // if entity is using action
        // continue
    }

    void FixedUpdate()
    {
        // animate method which animates through current animation set, return framePos
    }

}
